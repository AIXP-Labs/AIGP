# AIKP Navigator AIAP Example

This generated content package is part of
`examples/aikp_navigator_aiap_ainp/`. It is a tiny AIAP-style package used to
show how AINP can govern generation of an application package.

## Run

From this content package directory:

```bash
python -B tools/resolve_topic.py "Release evidence"
```

The command prints a JSON navigation request with a normalized topic slug.

## Test

```bash
python -B tests/test_resolve_topic.py
```

## Boundary

AINP validation proves package structure, hash recomputability and evidence
wiring for this generated example. It does not prove that an AIAP runtime
executed the package, that the package is production-ready or that the package
is externally trusted.

---

Generated example content. Align Axiom 0: Human Sovereignty and Wellbeing.
