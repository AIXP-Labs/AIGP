# AIAP — AIKP Navigator Example

This is a generated AIAP package declaration used as content inside an AINP
example project. It demonstrates how AINP can govern AIAP package generation.

## Purpose

The package turns a user topic into a normalized AIKP navigation request. It is
small enough to inspect and is intentionally local-only.

## Runtime Boundary

This file is an AIAP package declaration example. It is not AINP evidence, not
an AINP plan, and not proof that an AIAP runtime executed the package. The
surrounding AINP project records generation intent, artifact hashes, acceptance
evidence and feedback.

## Safety Boundary

The helper tool uses only the Python standard library. It does not perform
network access, file writes, subprocess launches or credential handling.

---

Generated example content. Align Axiom 0: Human Sovereignty and Wellbeing.
